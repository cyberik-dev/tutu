<p id="notice"><%= notice %></p>

<p>
  <strong>Название:</strong>
  <%= @railway_station.title %>

  <br><strong>Поезда на станции:</strong>
  <% if @railway_station.trains.empty? %>
    На станции нет поездов
  <% else %>
    <% @railway_station.trains.each do |train| %>
      <%= train.number %>, 
    <% end %>
  <% end %>
</p>

<% if @railway_station.routes.empty? %>
    <p>Станция не входит ни в один маршрут</p>
<% else %>
    <p>Станция входит в следующие маршруты:</p>
    <ul>
      <% @railway_station.routes.each do |route| %>
        <li><%= route.name %></li>
      <% end %>
    </ul>
<% end %>

<%= link_to 'Редактировать', edit_railway_station_path(@railway_station) %> |
<%= link_to 'Назад', railway_stations_path %>
